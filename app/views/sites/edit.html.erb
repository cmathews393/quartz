<div class="min-h-screen bg-gradient-to-br from-gray-900 via-gray-800 to-gray-950 p-8">
  <div class="max-w-2xl mx-auto">
    <h1 class="text-4xl font-bold text-white mb-8">Edit Site <span class="text-gray-400"><%= @site.id %></span></h1>

    <%= render "form", site: @site %>

    <%= link_to "Cancel", sites_path, class: "inline-block mt-6 text-gray-400 hover:text-white transition-colors" %>
  </div>
</div>
